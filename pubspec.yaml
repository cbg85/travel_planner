name: adoption_travel_planner
description: A Flutter app to manage adoption and travel plans with CRUD operations and an interactive UI.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5  # For optional state management if needed
  table_calendar: ^3.0.9  # For calendar support
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
